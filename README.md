# MAC_Changer
Learning project to change MAC address of a machine.<br/>
<b>Note: </b> MAC Address is hardcoded in NIC (Network Interface Card). However, MAC Addresses can be <b>spoofed</b> from the OS side.That I am going to do here.
